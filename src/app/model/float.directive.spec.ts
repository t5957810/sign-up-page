import { FloatDirective } from './float.directive';

describe('FloatDirective', () => {
  it('should create an instance', () => {
    const directive = new FloatDirective();
    expect(directive).toBeTruthy();
  });
});

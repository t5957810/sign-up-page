import { IconColorChangeDirective } from './icon-color-change.directive';

describe('IconColorChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new IconColorChangeDirective();
    expect(directive).toBeTruthy();
  });
});
